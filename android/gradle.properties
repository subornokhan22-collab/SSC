org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false
# R8 full mode's class merging / aggressive optimizations break the
# Play services scanner client (GmsDocumentScanning.getClient NPEs in
# release, works with minify off). Compat mode keeps shrinking but
# drops those transformations.
android.enableR8.fullMode=false
