# Phase 1 recovery — 2026-09-24

## Provenance

- Remote `arena/01a081ac-ssc` ends at `c30c528`; its tree equals PR #5's
  merge (`88a10f4`). The published branch did not contain the later Phase 1 work.
- `origin/main` at `f8e732f` adds `converted.txt`, the supplied cumulative patch.
- That patch contains `phase1-review-originals.html`, whose embedded ZIP recovered
  all **17 original files, seven patches, and PHASE1-README.txt**. Every ZIP file
  matched its destination Git blob hash declared in the supplied patch.
- ZIP SHA-256: `7181c7f9c337949dcdc6bbd03d2008f66371c043f67f477461549b6aff78794a`.
- Commit `42d3589` restores exactly the 17 original Phase 1 files on the current
  session branch. The unrelated cumulative-patch entries were not applied.

## Integration corrections (separate from the originals)

- Replaced a numeric digit separator requiring Dart language 3.6; the package
  currently declares language 3.0, so the size limit uses a compatible literal.
- Supabase JWT comes from `currentSession.accessToken`, not `currentUser`.
- The AI validator/detector and tests import the actual `data/questions_data.dart`.
- Validation's `valid` is a getter rather than a non-constant const initializer;
  fixed malformed interpolation in the duplicate error message.
- The RLS migration uses `pg_policies.policyname`; the audit no longer includes
  a psql-only command in a script intended for the Supabase SQL Editor.
- The RLS checklist now distinguishes a privileged SQL Editor connection from
  an authenticated app-user session.
- Added disposable PostgreSQL regression tests for policy replacement,
  idempotency, own-row access, blocked self-upgrades, and service-role grants.
- The gateway bounds actual streamed request bytes, rejects non-object JSON,
  and returns controlled errors for malformed bodies/auth verification failures.
  Request-boundary tests run without network access or API keys.
- Added the missing mounted check after clearing persisted MiMi history.
- Bank-health and format pipelines explicitly propagate command failure.

## Validation and deployment

The historical green CI run `35817971489` applies only to the pre-Phase-1 remote
branch. Fresh Phase 1 CI passed for `65dec3b`: run
[35976648526](https://github.com/subornokhan22-collab/SSC/actions/runs/35976648526)
completed analyze, Flutter tests, bank-health, Deno typecheck, request-boundary
tests, RLS regression tests, APK build and artifact upload successfully. SDK downloads from Google's storage
and the Flutter mirror were unavailable in this sandbox, so Flutter validation
must run on GitHub Actions. Local Node request-boundary tests and Python bank
health checks are available here.

No production SQL, Supabase function deployment, or secret configuration has
been performed by this recovery. GitHub's secrets metadata endpoint denied the
integration access (HTTP 403); existing secret configuration is unknown. Do not
paste secret values into chat. Follow the corrected RLS/signing checklists through
the owner dashboards or a securely authorized deployment connection.

Before public AI rollout, configure usage budgets/rate limiting: the recovered
gateway authenticates users and bounds payloads, but does not implement a shared,
persistent per-user quota. Bank health still reports the six known duplicate
stems; these are warnings, not errors, and editorial cleanup remains queued.

Phase 2 has now started after fresh Phase 1 CI went green (see
`docs/PHASE2-ARCHITECTURE.md`): design tokens, formatting gate,
then controllers/widgets for the three large screens, shared state/navigation,
and centralized bank validation. The original tracker/handoff are retained as
historical context; their original "complete" labels do not prove deployment.
